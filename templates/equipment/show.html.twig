{% extends 'base.html.twig' %}

{% block title %}Equipment{% endblock %}

{% block body %}
    <h1>Оборудование</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Наименование оборудования</th>
            <td>{{ equipment.name }}</td>
        </tr>
        <tr>
            <th>Описание</th>
            <td>{{ equipment.description }}</td>
        </tr>
        <tr>
            <th>График тех. индекса</th>
            <td>{{  render_chart(chartTech)  }}</td>
        </tr>
        <tr>
            <th>График Рэо</th>
            <td>{{  render_chart(chartRel)  }}</td>
        </tr>
        </tbody>

    </table>
    {{ include('techical_indexs/show.html.twig') }}
    {{ include('reliabilities/show.html.twig') }}
    <div class="m-2">
        <a class="btn btn-info" href="{{  path('find_tech_index',{'id':equipment.id}) }}">Расчет тех. индекса</a>
        <a class="btn btn-info" href="{{  path('reliability_equipment',{'id':equipment.id}) }}">Расчет Рэо</a>
    </div>
    {#  <a href="{{ path('equipment_index') }}">back to list</a>

    <a href="{{ path('equipment_edit', {'id': equipment.id}) }}">edit</a>
    {{ include('equipment/_delete_form.html.twig') }} #}
{% endblock %}
