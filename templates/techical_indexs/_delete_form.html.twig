<form method="post" action="{{ path('techical_indexs_delete', {'id': techical_index.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ techical_index.id) }}">
    <button class="btn">Delete</button>
</form>
