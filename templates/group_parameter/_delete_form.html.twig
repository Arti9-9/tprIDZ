<form method="post" action="{{ path('group_parameter_delete', {'id': group_parameter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group_parameter.id) }}">
    <button class="btn">Delete</button>
</form>
