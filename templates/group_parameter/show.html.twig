{% extends 'base.html.twig' %}

{% block title %}GroupParameter{% endblock %}

{% block body %}
    <h1>GroupParameter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group_parameter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ group_parameter.name }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ group_parameter.weight }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('group_parameter_index') }}">back to list</a>

    <a href="{{ path('group_parameter_edit', {'id': group_parameter.id}) }}">edit</a>

    {{ include('group_parameter/_delete_form.html.twig') }}
{% endblock %}
