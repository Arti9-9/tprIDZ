{% extends 'base.html.twig' %}

{% block title %}FunctionalUnit{% endblock %}

{% block body %}
    <h1>FunctionalUnit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ functional_unit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ functional_unit.name }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ functional_unit.weight }}</td>
            </tr>
            <tr>
                <th>Appraisal</th>
                <td>{{ functional_unit.appraisal }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ functional_unit.date ? functional_unit.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('functional_unit_index') }}">back to list</a>

    <a href="{{ path('functional_unit_edit', {'id': functional_unit.id}) }}">edit</a>

    {{ include('functional_unit/_delete_form.html.twig') }}
{% endblock %}
